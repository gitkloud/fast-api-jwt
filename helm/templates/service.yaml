apiVersion: v1
kind: Service
metadata:
  name: {{ include "fast-api-jwt.fullname" . }}
  labels:
    {{- include "fast-api-jwt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fast-api-jwt.selectorLabels" . | nindent 4 }}
